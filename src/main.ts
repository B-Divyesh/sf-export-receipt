import './style.css';
import './asset-overrides.css';
import { inspectFile, receiptData, receiptPayload, sampleInspection, verifyReceipt } from './inspect';
import type { Inspection } from './types';
import heroArt from './archive-workbench.webp';

const app = document.querySelector<HTMLDivElement>('#app')!;
const origin = 'https://export-receipt.sociobot.in';
let inspection: Inspection | null = null;
let demo = location.pathname === '/demo' || new URLSearchParams(location.search).get('demo') === '1';
let savedDark = localStorage.getItem('export-receipt:theme') === 'dark';
let dark = savedDark;
let focusHeadingAfterRender = false;

const bytes = (n: number) => n < 1024 ? `${n} B` : n < 1024 ** 2 ? `${(n / 1024).toFixed(1)} KB` : `${(n / 1024 ** 2).toFixed(1)} MB`;
const escape = (v: string) => v.replace(/[&<>\"]/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c]!));
type PageMeta = { title: string; description: string; path: string };

function setMeta(meta: PageMeta) {
  document.title = meta.title;
  const url = `${origin}${meta.path}`;
  const values: Array<[string, string, string]> = [
    ['name', 'description', meta.description], ['rel', 'canonical', url], ['property', 'og:title', meta.title], ['property', 'og:description', meta.description], ['property', 'og:url', url], ['name', 'twitter:title', meta.title], ['name', 'twitter:description', meta.description],
  ];
  for (const [attribute, key, value] of values) {
    const selector = attribute === 'rel' ? `link[rel="${key}"]` : `meta[${attribute}="${key}"]`;
    document.querySelector(selector)?.setAttribute(attribute === 'rel' ? 'href' : 'content', value);
  }
}
function route(path: string) { history.pushState({}, '', path); focusHeadingAfterRender = true; render(); }
window.addEventListener('popstate', () => { focusHeadingAfterRender = true; render(); });

function shell(content: string, meta: PageMeta, h1: string) {
  setMeta(meta);
  app.dataset.theme = dark ? 'dark' : 'light';
  app.innerHTML = `<header class="site-header"><a class="wordmark" href="/" data-link>EXPORT <span>RECEIPT</span></a><nav aria-label="Primary"><a href="/demo" data-link>Demo</a><a href="/#how">How it works</a><a href="/privacy" data-link>Privacy</a><button class="theme-toggle" type="button">Use ${dark ? 'light' : 'dark'} colors</button></nav></header><div id="announce" class="sr-only" aria-live="polite"></div><main id="main" tabindex="-1">${content}</main><footer><p>Export Receipt checks local exports before access disappears.</p><p><a href="/privacy" data-link>Privacy</a> · <a href="/terms" data-link>Terms</a> · Built by Param Factory · v1.0.1</p></footer><div id="update-toast" hidden><span>An update is ready.</span><button type="button">Reload</button></div>`;
  app.querySelectorAll<HTMLAnchorElement>('[data-link]').forEach((link) => link.addEventListener('click', (event) => { event.preventDefault(); if (link.hash) { route(`${link.pathname}${link.hash}`); queueMicrotask(() => document.querySelector(link.hash)?.scrollIntoView()); } else route(link.pathname); }));
  app.querySelector('.theme-toggle')?.addEventListener('click', () => { dark = !dark; if (!demo) { savedDark = dark; localStorage.setItem('export-receipt:theme', dark ? 'dark' : 'light'); } render(); });
  app.querySelector('#update-toast button')?.addEventListener('click', () => location.reload());
  const shouldFocusHeading = focusHeadingAfterRender;
  focusHeadingAfterRender = false;
  queueMicrotask(() => { if (shouldFocusHeading) { const heading = document.querySelector<HTMLElement>('h1'); if (heading) { heading.tabIndex = -1; heading.focus(); } } document.querySelector('#announce')!.textContent = h1; });
}

function demoBar() { return demo ? `<aside class="demo-bar" aria-label="Demo controls" data-claim="demo-isolation"><strong>Demo — sample data, nothing is saved</strong><button data-reset>Reset demo</button><button data-real>Start for real</button></aside>` : ''; }
function landing() {
  shell(`<section class="hero"><div class="hero-copy"><p class="kicker">LOCAL EXPORT CHECK</p><h1>Check your export before access ends</h1><p class="lede">For people leaving a service, see what your export contains before an account disappears.</p><div class="hero-actions"><button class="primary" data-demo>Try it with sample data</button><span data-claim="sample-inventory">Loads a sample receipt now.</span></div><ul class="facts"><li data-claim="local-only">Runs in your browser</li><li data-claim="offline-reload">Works offline after first visit</li><li data-claim="account-free">No account needed</li></ul></div><figure class="hero-art"><img src="${heroArt}" width="768" height="768" fetchpriority="high" decoding="async" alt="An export folder, magnifier, and printed data receipt on a workbench."><figcaption>Generated product illustration: inspect the export, then keep the receipt.</figcaption></figure></section><section class="workbench" aria-labelledby="bench-title"><div><p class="kicker">THE RECEIPT DESK</p><h2 id="bench-title">Open an export to make a receipt</h2><p data-claim="supported-formats local-only">Choose a ZIP, JSON, CSV, or text export. Nothing is uploaded.</p></div><label class="drop-zone" for="archive"><span>Choose an export</span><small>ZIP · JSON · CSV · TXT</small><input id="archive" type="file" accept=".zip,.json,.csv,.txt,text/plain,application/zip" /></label><p class="error" role="status" aria-live="polite"></p></section><section id="how" class="steps" aria-labelledby="how-title"><h2 id="how-title">How Export Receipt checks an export</h2><ol><li><strong>Choose</strong><span>Open an export on this device.</span></li><li><strong>Inspect</strong><span data-claim="sample-inventory parse-errors safe-archive-limits recognized-layouts">Count files, dates, supported categories, attachments, and parse errors.</span></li><li><strong>Keep</strong><span data-claim="json-receipt html-receipt receipt-verification">Download and verify a locally signed HTML or JSON receipt.</span></li></ol></section><section class="limits" aria-labelledby="limits-title"><h2 id="limits-title">What this does not do</h2><p data-claim="account-free local-only">It inspects only the export you choose. It does not connect to your service account.</p></section>`, { title: 'Export Receipt — Check your data export', description: 'Inspect a data export before you lose access. Get a local receipt of what is present, missing, and readable.', path: '/' }, 'Check your export before access ends');
  app.querySelector('[data-demo]')?.addEventListener('click', enterDemo);
  app.querySelector<HTMLInputElement>('#archive')?.addEventListener('change', handleFile);
}
function report() {
  if (!inspection) return receiptEmpty();
  const readable = inspection.files.filter((f) => f.readable);
  const dateFinding = inspection.findings.find((f) => f.title.startsWith('Date coverage'));
  const checks = inspection.categoryChecks.filter((item) => item.status !== 'present');
  shell(`${demoBar()}<section class="report-head"><div><p class="kicker">INSPECTION RECEIPT</p><h1>Your export at a glance</h1><p class="lede">Keep this with the original export. Recheck warnings before you close an account.</p></div><div class="stamp">${inspection.source === 'sample' ? 'SAMPLE' : 'CHECKED'}<small>${new Date(inspection.inspectedAt).toLocaleDateString()}</small></div></section><section class="summary" aria-label="Inspection summary"><article><span>Export</span><strong>${escape(inspection.name)}</strong><small data-claim="source-hash">${bytes(inspection.bytes)} · SHA-256 ${inspection.hash.slice(0, 12)}…</small></article><article><span>Readable files</span><strong>${readable.length}</strong><small>Successfully parsed JSON or CSV</small></article><article><span>Date coverage</span><strong>${dateFinding ? escape(dateFinding.title.replace('Date coverage: ', '')) : 'Not found'}</strong><small>From valid dates in readable content</small></article></section><div class="report-grid"><section class="receipt" aria-labelledby="findings-title" data-claim="sample-inventory"><div class="section-title"><h2 id="findings-title">What needs your attention</h2><span>${inspection.findings.filter((f) => f.level !== 'ok' && f.level !== 'info').length} checks</span></div><ul class="findings">${inspection.findings.map((f) => `<li class="${f.level}"><b>${f.level === 'ok' ? '✓' : f.level === 'warn' ? '!' : f.level === 'danger' ? '×' : 'i'}</b><div><strong>${escape(f.title)}</strong><p>${escape(f.detail)}</p></div></li>`).join('')}</ul>${inspection.inspector ? `<p class="inspector-note" data-claim="recognized-layouts">Layout matched: <strong>${escape(inspection.inspector)}</strong>. ${checks.length ? `${checks.length} category checks need confirmation.` : 'Expected categories are present.'}</p>` : ''}</section><aside class="checklist"><h2>Retest checklist</h2><ol>${receiptPayload(inspection).retestChecklist.map((x) => `<li>${escape(x)}</li>`).join('')}</ol><button class="secondary" data-json data-claim="json-receipt">Download signed JSON receipt</button><button class="primary" data-html data-claim="html-receipt">Download signed HTML receipt</button><button class="text-button" data-another>Check another export</button></aside></div><section class="verify-receipt" aria-labelledby="verify-title" data-claim="receipt-verification"><h2 id="verify-title">Verify a signed JSON receipt</h2><p>Choose a downloaded JSON receipt. This checks whether it changed after signing.</p><p>The signer is this local browser, not a trusted service identity.</p><label class="verify-picker" for="verify-receipt">Choose a signed JSON receipt<input id="verify-receipt" type="file" accept="application/json,.json"></label><p id="verification-result" role="status" aria-live="polite"></p></section><section class="inventory" aria-labelledby="inventory-title"><h2 id="inventory-title">File inventory</h2><div class="table-wrap" tabindex="0" role="region" aria-label="File inventory. Use arrow keys to scroll the table."><table><thead><tr><th>Path</th><th>Type</th><th>Records</th><th>Size</th></tr></thead><tbody>${inspection.files.map((f) => `<tr><td>${escape(f.path)}</td><td>${f.kind}</td><td>${f.records ?? '—'}</td><td>${bytes(f.bytes)}</td></tr>`).join('')}</tbody></table></div></section>`, demo ? { title: 'Demo — Export Receipt', description: 'Try the Harbor Mail sample export. Nothing is saved.', path: '/demo' } : { title: 'Receipt — Export Receipt', description: 'Review a local export receipt and its next checks.', path: '/receipt' }, 'Your export at a glance');
  app.querySelector('[data-reset]')?.addEventListener('click', enterDemo);
  app.querySelector('[data-real]')?.addEventListener('click', () => { demo = false; dark = savedDark; inspection = null; route('/'); });
  app.querySelector('[data-json]')?.addEventListener('click', async () => download('export-receipt.json', JSON.stringify(await receiptData(inspection!), null, 2), 'application/json'));
  app.querySelector('[data-html]')?.addEventListener('click', async () => download('export-receipt.html', receiptHtml(await receiptData(inspection!)), 'text/html'));
  app.querySelector('[data-another]')?.addEventListener('click', () => { inspection = null; render(); });
  app.querySelector<HTMLInputElement>('#verify-receipt')?.addEventListener('change', verifyUploadedReceipt);
}
function receiptEmpty() { shell(`<article class="legal receipt-empty"><p class="kicker">NO OPEN RECEIPT</p><h1>No receipt is open</h1><p>Choose an export to make a receipt, or use the sample receipt first.</p><p class="actions"><a class="button-link primary-link" href="/">Choose an export</a><a class="button-link" href="/demo" data-link>Try the sample receipt</a></p></article>`, { title: 'No receipt is open — Export Receipt', description: 'Choose an export or open the sample receipt.', path: '/receipt' }, 'No receipt is open'); }
function info(kind: 'privacy' | 'terms') {
  const privacy = kind === 'privacy';
  shell(`<article class="legal"><p class="kicker">${privacy ? 'LOCAL BY DEFAULT' : 'PLAIN TERMS'}</p><h1>${privacy ? 'Your export stays on your device' : 'Use Export Receipt at your own pace'}</h1>${privacy ? '<p data-claim="local-only account-free">Export Receipt reads exports in your browser. It does not upload exports, use accounts, run analytics, or call a server API.</p><h2>Storage</h2><p data-claim="preference-storage demo-isolation">Your color choice is the only browser preference stored. Demo data stays in memory and disappears when you leave the demo.</p><h2>Questions</h2><p>Contact the Param Factory through its product listing.</p>' : '<p>Export Receipt is a local inspection tool. It gives technical observations, not legal, security, or compliance advice.</p><h2>Your files</h2><p>You remain responsible for keeping your export and checking whether it meets your needs.</p><h2>No warranty</h2><p>The software is provided as-is under the MIT License.</p>'}</article>`, privacy ? { title: 'Privacy — Export Receipt', description: 'Read how Export Receipt keeps exports on your device.', path: '/privacy' } : { title: 'Terms — Export Receipt', description: 'Read the terms for using Export Receipt.', path: '/terms' }, privacy ? 'Your export stays on your device' : 'Use Export Receipt at your own pace');
}
async function enterDemo() { demo = true; inspection = await sampleInspection(); route('/demo'); }
async function handleFile(event: Event) { const file = (event.target as HTMLInputElement).files?.[0]; if (!file) return; const msg = app.querySelector<HTMLElement>('.error')!; msg.textContent = 'Inspecting this export…'; try { inspection = await inspectFile(file); demo = false; route('/receipt'); } catch (error) { msg.textContent = error instanceof Error ? error.message : 'The export could not be inspected. Choose another export.'; } }
async function verifyUploadedReceipt(event: Event) { const result = app.querySelector<HTMLElement>('#verification-result')!; const file = (event.target as HTMLInputElement).files?.[0]; if (!file) return; try { const signed = JSON.parse(await file.text()); result.textContent = await verifyReceipt(signed) ? 'Valid signature. This receipt has not changed since this browser signed it.' : 'Invalid signature. This receipt changed or is not a signed Export Receipt JSON file.'; } catch { result.textContent = 'This is not a readable signed Export Receipt JSON receipt. Choose another downloaded receipt.'; } }
function download(name: string, content: string, type: string) { const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob([content], { type })); a.download = name; a.click(); setTimeout(() => URL.revokeObjectURL(a.href), 1000); }
function receiptHtml(data: Awaited<ReturnType<typeof receiptData>>) { return `<!doctype html><html lang="en"><meta charset="utf-8"><title>Export Receipt — ${escape(data.name)}</title><style>body{font:16px system-ui;max-width:800px;margin:48px auto;padding:0 24px;color:#18211d}h1{font-size:2rem}li{margin:12px 0}table{border-collapse:collapse;width:100%}td,th{border:1px solid #18211d;padding:8px;text-align:left}</style><h1>Export Receipt</h1><p>Export: ${escape(data.name)}</p><p>SHA-256: ${data.hash}</p><p>Signed by: ${escape(data.signature.signer)} (${escape(data.signature.algorithm)})</p><p>Signature: ${escape(data.signature.value)}</p><h2>Findings</h2><ul>${data.findings.map((f) => `<li><strong>${escape(f.title)}</strong> — ${escape(f.detail)}</li>`).join('')}</ul><h2>Retest checklist</h2><ol>${data.retestChecklist.map((x) => `<li>${escape(x)}</li>`).join('')}</ol></html>`; }
function notFound() { shell(`<article class="legal"><p class="kicker">404</p><h1>That page is not here</h1><p>Use the home page to inspect an export or open the sample receipt.</p><p><a class="button-link primary-link" href="/">Return home</a></p></article>`, { title: 'Page not found — Export Receipt', description: 'This Export Receipt page is not available.', path: '/404' }, 'That page is not here'); }
function render() { if (demo && location.pathname === '/') history.replaceState({}, '', '/demo'); if (location.pathname === '/privacy') return info('privacy'); if (location.pathname === '/terms') return info('terms'); if (location.pathname === '/demo' && !inspection) { demo = true; landing(); void sampleInspection().then((result) => { inspection = result; report(); }); return; } if (location.pathname === '/demo') return report(); if (location.pathname === '/receipt') return report(); if (location.pathname === '/') return landing(); notFound(); }
render();
if ('serviceWorker' in navigator) window.addEventListener('load', async () => { const registration = await navigator.serviceWorker.register('/sw.js'); registration.addEventListener('updatefound', () => { const worker = registration.installing; worker?.addEventListener('statechange', () => { if (worker.state === 'installed' && navigator.serviceWorker.controller) app.querySelector<HTMLElement>('#update-toast')!.hidden = false; }); }); });
